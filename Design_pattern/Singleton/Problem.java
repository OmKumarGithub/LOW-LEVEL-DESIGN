package Singleton;

public class Problem {
    //I don't think it should be considered as a design pattern it is a situation
    // The problem is we have to bound user to only choose single instance no matter how many instructors user makes it should always be referred to a single reference of object
}
